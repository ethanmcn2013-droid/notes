# Claude Code Instructions — Signal Notes

Read `AGENTS.md` and `docs/PRODUCT.md` first.

For any UI work, the source of truth is **Signal Design System 2.0** — the `signal-design-system` repo (`docs/` + `tokens/tokens.css`), vendored here at `src/ds/`. Notes's former green/mustard/Inter aesthetic was retired by operator decision on 2026-07-02 (design-system mandate, Checkpoint 2 + Wave 4): Notes now shares the suite register — white paper, one indigo, Geist. Its identity lives in the `notes.` wordmark, the caret gesture, and the capture-first surface, not in colour. Do not reintroduce the green register; `ds:check` (CI) will reject it. The old `studio/DESIGN.md` §11 note is superseded.

Claude Code must follow the Signal HQ sync rule: meaningful Notes changes must be reflected in the Studio repo's Signal HQ data before the task is complete.

Before note extraction, decision, action, risk, sharing, guest-facing, or cross-product work, read `docs/COLLABORATION_LOOP.md`.
